import React from 'react';
import 'Auth.css'

function Auth(props) {
    return (
        <div className="authContainer">
            {/* //Not coded yet */}
        </div>
    );
}

export default Auth;